
Install all packages by running: npm install

//For production no need to do this
/----------------------
Install mongod and robomongo
create DBpath folder
run this command:
mongod --dbpath <DBpath>
//-----------------------

run: webpack
run: npm run start

Heroku Deployment URL:

Open URL to see the project:
https://still-cliffs-75954.herokuapp.com/
